#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int size;
    int temp;
    int arr[1000];
    char dummy = 4; 
    int i, j;
    
    cin >> size;
    for (i = 0; i < size; i++){
       cin >> temp;
       arr[i] = temp;
    }
    for (j = size-1; j >= 0; j--){
       cout << arr[j] << " ";
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}

